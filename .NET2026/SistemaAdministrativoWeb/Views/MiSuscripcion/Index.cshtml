@model SistemaAdministrativoWeb.ViewModels.Suscripciones.MiSuscripcionIndexViewModel
@* Firma: FRANCO LARA - 08/07/2026 | Agrega la vista Mi suscripcion al panel contable con resumen de vigencia, limites operativos y contexto de cuenta administradora. *@
@* Firma: FRANCO LARA - 25/07/2026 | Mantiene un ancho de lectura controlado, simplifica las tarjetas y muestra visualmente el plan y estado efectivo, incluyendo alertas de gracia o bloqueo por vencimiento. *@
@* Firma: FRANCO LARA - 31/07/2026 | Adopta el patron global reutilizable de titulos y subtitulos, centra los KPI, agrega iconos a los datos de la cuenta e incorpora los canales de soporte. *@
@* Firma: FRANCO LARA - 03/09/2026 | Actualiza el correo visible y el enlace del canal de soporte comercial. *@
@* Firma: FRANCO LARA - 22/09/2026 | Incorpora las cuentas bancarias BCP para el pago de la suscripcion, con razon social, RUC, botones compactos de copiado alineados a la derecha y aviso de envio de constancia. *@
@{
    ViewData["Title"] = "Mi suscripción";
    ViewData["AdminShell"] = true;

    var estadoClase = Model.EstadoEfectivo switch
    {
        "ACTIVO" => "subscription-pill is-success",
        "EN_PRUEBA" => "subscription-pill is-warning",
        "EN_GRACIA" => "subscription-pill is-warning",
        "VENCIDO" => "subscription-pill is-danger",
        "SUSPENDIDO" => "subscription-pill is-danger",
        "BAJA" => "subscription-pill is-danger",
        _ => "subscription-pill"
    };

    var diasTexto = Model.DiasParaVencer switch
    {
        < 0 => $"Vencio hace {Math.Abs(Model.DiasParaVencer.Value)} dias",
        0 => "Vence hoy",
        > 0 => $"{Model.DiasParaVencer.Value} dias",
        _ => "Sin fecha"
    };
}

<section class="dashboard-shell subscription-shell dashboard-shell-contained dashboard-shell-contained-reading">
    <section class="dashboard-hero">
        <div>
            <span class="dashboard-kicker">General</span>
            <h1 class="app-page-title"><i class="bi bi-credit-card-2-front" aria-hidden="true"></i><span>Mi suscripción</span></h1>
            <p>Consulta la vigencia de la cuenta administradora, los límites operativos y el contexto de servicio de la empresa activa.</p>
        </div>
        <article class="dashboard-status-card">
            <span>Cuenta administradora</span>
            <strong>@Model.NombreCuenta</strong>
        </article>
    </section>

    @if (Model.AccesoRestringido)
    {
        <div class="alert alert-danger subscription-access-alert" role="alert">
            <strong>Acceso operativo restringido.</strong>
            <span>@Model.ResumenVigencia</span>
            <span>Mientras regularizas la suscripcion puedes consultar esta pantalla, la ayuda y cerrar sesion.</span>
        </div>
    }
    else if (string.Equals(Model.EstadoEfectivo, "EN_GRACIA", StringComparison.OrdinalIgnoreCase))
    {
        <div class="alert alert-warning subscription-access-alert" role="status">
            <strong>Periodo de gracia activo.</strong>
            <span>@Model.ResumenVigencia</span>
        </div>
    }

    <section class="dashboard-metrics">
        <article class="dashboard-metric-card dashboard-metric-card-text">
            <span>Estado actual</span>
            <strong><span class="@estadoClase">@Model.EstadoSuscripcion</span></strong>
        </article>
        <article class="dashboard-metric-card dashboard-metric-card-text">
            <span>Plan</span>
            <strong>@Model.TipoPlan</strong>
        </article>
        <article class="dashboard-metric-card dashboard-metric-card-text">
            <span>Fecha de vencimiento</span>
            <strong>@(Model.FechaVencimiento?.ToString("dd/MM/yyyy") ?? "Sin fecha")</strong>
        </article>
        <article class="dashboard-metric-card dashboard-metric-card-count">
            <span>Dias para vencer</span>
            <strong>@diasTexto</strong>
        </article>
    </section>

    <section class="workspace-section" aria-labelledby="subscription-account-contact-title">
        <partial name="_AdminSectionHeader" model="@(new AdminSectionHeaderViewModel
            {
                Id = "subscription-account-contact-title",
                Category = "Contexto",
                Title = "Datos de contacto de la cuenta",
                Description = "Correo y teléfono principal registrados para la cuenta administradora.",
                IconClass = "bi bi-person-vcard"
            })" />

        <div class="subscription-info-grid subscription-info-grid-compact">
            <article class="subscription-info-card subscription-contact-card">
                <span class="subscription-contact-icon" aria-hidden="true"><i class="bi bi-envelope"></i></span>
                <div class="subscription-contact-copy">
                    <span class="subscription-info-label">Correo principal</span>
                    <strong>@(string.IsNullOrWhiteSpace(Model.CorreoPrincipal) ? "No configurado" : Model.CorreoPrincipal)</strong>
                </div>
            </article>
            <article class="subscription-info-card subscription-contact-card">
                <span class="subscription-contact-icon" aria-hidden="true"><i class="bi bi-telephone"></i></span>
                <div class="subscription-contact-copy">
                    <span class="subscription-info-label">Telefono principal</span>
                    <strong>@(string.IsNullOrWhiteSpace(Model.TelefonoPrincipal) ? "No configurado" : Model.TelefonoPrincipal)</strong>
                </div>
            </article>
        </div>
    </section>

    <section class="workspace-section" aria-labelledby="subscription-capacity-title">
        <partial name="_AdminSectionHeader" model="@(new AdminSectionHeaderViewModel
            {
                Id = "subscription-capacity-title",
                Category = "Límites",
                Title = "Capacidad operativa",
                Description = "Referencia de crecimiento permitida para empresas y usuarios bajo la misma cuenta administradora.",
                IconClass = "bi bi-speedometer2"
            })" />

        <div class="subscription-limit-grid subscription-limit-grid-compact">
            <article class="dashboard-indicator-card">
                <div class="dashboard-indicator-icon">
                    <i class="bi bi-buildings"></i>
                </div>
                <div class="dashboard-indicator-copy">
                    <span>Empresas permitidas</span>
                    <strong>@(Model.EmpresasPermitidas?.ToString() ?? "Ilimitado")</strong>
                </div>
            </article>
            <article class="dashboard-indicator-card">
                <div class="dashboard-indicator-icon">
                    <i class="bi bi-people"></i>
                </div>
                <div class="dashboard-indicator-copy">
                    <span>Usuarios permitidos</span>
                    <strong>@(Model.UsuariosPermitidos?.ToString() ?? "Ilimitado")</strong>
                </div>
            </article>
            <article class="dashboard-indicator-card">
                <div class="dashboard-indicator-icon">
                    <i class="bi bi-patch-check"></i>
                </div>
                <div class="dashboard-indicator-copy">
                    <span>Estado de cuenta</span>
                    <strong>@(Model.EstadoCuenta ? "Activa" : "Inactiva")</strong>
                    <small>Valida si la cuenta administradora sigue habilitada para operar.</small>
                </div>
            </article>
        </div>
    </section>

    <section class="workspace-section" aria-labelledby="subscription-bank-accounts-title">
        <partial name="_AdminSectionHeader" model="@(new AdminSectionHeaderViewModel
            {
                Id = "subscription-bank-accounts-title",
                Category = "Pagos",
                Title = "Cuentas bancarias",
                Description = "Datos para realizar el pago de tu suscripcion mediante BCP.",
                IconClass = "bi bi-bank"
            })" />

        <div class="subscription-limit-grid subscription-limit-grid-compact">
            <article class="dashboard-indicator-card">
                <div class="dashboard-indicator-icon"><i class="bi bi-bank"></i></div>
                <div class="dashboard-indicator-copy">
                    <span>Banco</span>
                    <strong>@Model.BancoPago</strong>
                    <small>Cuenta en @Model.MonedaCuentaPago.</small>
                </div>
            </article>
            <article class="dashboard-indicator-card">
                <div class="dashboard-indicator-icon"><i class="bi bi-credit-card"></i></div>
                <div class="dashboard-indicator-copy">
                    <span>Número de cuenta</span>
                    <div class="d-flex align-items-center justify-content-between gap-2">
                        <strong class="text-break">@Model.NumeroCuentaPago</strong>
                        <button type="button" class="btn btn-outline-primary btn-sm subscription-copy-button" data-copy-value="@Model.NumeroCuentaPago" data-copy-label="Copiar número de cuenta" aria-label="Copiar número de cuenta" title="Copiar número de cuenta"><i class="bi bi-copy" aria-hidden="true"></i></button>
                    </div>
                </div>
            </article>
            <article class="dashboard-indicator-card">
                <div class="dashboard-indicator-icon"><i class="bi bi-arrow-left-right"></i></div>
                <div class="dashboard-indicator-copy">
                    <span>Cuenta interbancaria (CCI)</span>
                    <div class="d-flex align-items-center justify-content-between gap-2">
                        <strong class="text-break">@Model.NumeroCuentaInterbancariaPago</strong>
                        <button type="button" class="btn btn-outline-primary btn-sm subscription-copy-button" data-copy-value="@Model.NumeroCuentaInterbancariaPago" data-copy-label="Copiar CCI" aria-label="Copiar CCI" title="Copiar CCI"><i class="bi bi-copy" aria-hidden="true"></i></button>
                    </div>
                </div>
            </article>
            <article class="dashboard-indicator-card">
                <div class="dashboard-indicator-icon"><i class="bi bi-building-check"></i></div>
                <div class="dashboard-indicator-copy">
                    <span>Titular</span>
                    <strong>@Model.RazonSocialPago</strong>
                    <small>RUC: @Model.RucPago</small>
                </div>
            </article>
        </div>
        <div class="alert alert-info mt-3 mb-0" role="note">
            <i class="bi bi-info-circle me-1" aria-hidden="true"></i>
            Luego de realizar el pago, envía tu constancia por WhatsApp indicando el nombre de tu negocio.
        </div>
    </section>

    <section class="workspace-section subscription-support-section" aria-labelledby="subscription-support-title">
        <partial name="_AdminSectionHeader" model="@(new AdminSectionHeaderViewModel
            {
                Id = "subscription-support-title",
                Category = "Contacto",
                Title = "Soporte de la plataforma",
                Description = "Canales de atención para consultas, ampliaciones y coordinaciones directas.",
                IconClass = "bi bi-headset"
            })" />

        <div class="subscription-support-grid">
            <a class="subscription-support-card" href="mailto:contacto@fralsetech.com">
                <span class="subscription-contact-icon" aria-hidden="true"><i class="bi bi-envelope"></i></span>
                <span class="subscription-contact-copy">
                    <span class="subscription-info-label">Correo</span>
                    <strong>contacto@fralsetech.com</strong>
                    <small>Canal principal para consultas, ampliaciones y soporte operativo.</small>
                </span>
            </a>
            <a class="subscription-support-card" href="https://wa.me/51937528701" target="_blank" rel="noopener noreferrer" aria-label="Contactar al soporte por WhatsApp al +51 937 528 701">
                <span class="subscription-contact-icon" aria-hidden="true"><i class="bi bi-whatsapp"></i></span>
                <span class="subscription-contact-copy">
                    <span class="subscription-info-label">WhatsApp</span>
                    <strong>+51 937 528 701</strong>
                    <small>Línea de atención para coordinaciones directas con plataforma.</small>
                </span>
            </a>
        </div>
    </section>

</section>

@section Scripts {
    <script>
        (() => {
            document.querySelectorAll('.subscription-copy-button').forEach((button) => {
                button.addEventListener('click', async () => {
                    try {
                        await navigator.clipboard.writeText(button.dataset.copyValue || '');
                        button.innerHTML = '<i class="bi bi-check2" aria-hidden="true"></i>';
                        button.setAttribute('aria-label', 'Dato copiado');
                        button.title = 'Dato copiado';
                        window.setTimeout(() => {
                            button.innerHTML = '<i class="bi bi-copy" aria-hidden="true"></i>';
                            button.setAttribute('aria-label', button.dataset.copyLabel || 'Copiar');
                            button.title = button.dataset.copyLabel || 'Copiar';
                        }, 1800);
                    } catch {
                        button.focus();
                    }
                });
            });
        })();
    </script>
}
